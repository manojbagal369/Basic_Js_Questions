/*
5. Count Vowels in a String
Problem: Return the number of vowels in a given string.
Test Cases:
countVowels("hello") ➞ 2
countVowels("xyz") ➞ 0
countVowels("AEIOU") ➞ 5
*/

function countVowels(str) {
  function vowels(arr) {}
  console.log(vowels(["a", "e", "i", "o", "u", "A", "E", "I", "O", "U"]));
  let count = 0;

  for (let i = 0; i < str.length; i++) {
    console.log(str[i]);
    if (countVowels.find(vowels));
    {
      count++;
    }
  }
  return count;
}
console.log(countVowels("hello"));
console.log(countVowels("xyz"));
